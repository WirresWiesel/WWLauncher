<Window x:Class="Launcher.Views.AddProgramWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Programm hinzufügen"
        Height="235"
        Width="360"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Pfad -->
        <TextBlock Text="Programmdatei"
                   FontWeight="SemiBold"
                   Margin="0,0,0,6"/>

        <DockPanel Grid.Row="1" Margin="0,0,0,12">
            <Button x:Name="BtnChoosePath"
                    Content="Durchsuchen…"
                    DockPanel.Dock="Right"
                    Width="90"
                    Margin="8,0,0,0"
                    Click="BtnChoosePath_Click"/>

            <TextBox x:Name="TxtPath"
                     IsReadOnly="True"
                     VerticalContentAlignment="Center"
                     Padding="6"/>
        </DockPanel>

        <!-- Name -->
        <StackPanel Grid.Row="2" Margin="0,0,0,16">
            <TextBlock Text="Anzeigename"
                       FontWeight="SemiBold"
                       Margin="0,0,0,6"/>
            <TextBox x:Name="TxtBoxCustomName"
                     Padding="6"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">

            <Button x:Name="BtnCancel"
                    Content="Abbrechen"
                    Width="90"
                    Margin="0,0,8,0"
                    Click="BtnCancel_Click"/>

            <Button x:Name="BtnOk"
                    Content="OK"
                    Width="90"
                    IsDefault="True"
                    Click="BtnOk_Click"/>
        </StackPanel>
    </Grid>
</Window>
